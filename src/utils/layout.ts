// Keep this in sync with the sidebar breakpoint in styles.css (`@media(max-width:1050px)`).
// Below this width the sidebar becomes an off-canvas drawer; at/above it, it's a persistent column.
export const MOBILE_BREAKPOINT = 1050
